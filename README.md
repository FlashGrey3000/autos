# Game Simulations
Game Simulations... I just wanted lots of data...  

## Data Storage
The data from each match is recorded and stored in `postgres`, which enables further investigations and observing patterns.  

### database.ini
your `database.ini` should look something like this:

```ini
[postgresql]
host=localhost
user=youruser
password=yourpassword
database=yourdatabasename 
```